const button = require('@base-ui/react/button');
console.log('Button:', Object.keys(button));

const badge = require('@base-ui/react/badge');
console.log('Badge:', Object.keys(badge));

const input = require('@base-ui/react/input');
console.log('Input:', Object.keys(input));
